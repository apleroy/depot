//#START_HIGHLIGHT
//$( document ).ready(function() {
	$("#notice").hide();


    if ($('#cart tr').length == 1) { $('#cart').show('blind', 0); }

	//#END_HIGHLIGHT
	$('#cart').html("<%= escape_javascript render(@cart) %>");
//});
